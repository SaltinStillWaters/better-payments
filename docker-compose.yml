services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
    restart: unless-stopped

  # Uncomment to run a local Stellar network for integration testing.
  # stellar:
  #   image: stellar/quickstart:testing
  #   command: ["--testnet", "--enable-soroban-rpc"]
  #   ports:
  #     - "8000:8000"
  #   volumes:
  #     - stellar-data:/var/lib/stellar

# volumes:
#   stellar-data:
